@using EcommerceWebsite.Models.Cart
@model Cart

<div class="header__cart">
    <ul>
        <li><a asp-controller="Cart"><i class="fa fa-shopping-bag"></i> <span>@Model?.Lines.Count</span></a></li>
    </ul>
    <div class="header__cart__price">item: <span>@Model?.ComputeTotalValue()</span></div>
</div>